# Minimal Postgres SSL config for testing.
# Included via -c ssl=on and related flags in docker-compose.test.yml.
ssl = on
ssl_cert_file = '/etc/ssl/certs/server.crt'
ssl_key_file = '/etc/ssl/private/server.key'
